#include "binary_trees.h"

/**
 * array_to_bst - a function
 *
 * @array: binary tree to go through
 * @size: value
 *
 * Return: If tree is NULL, do nothing
 */
bst_t *array_to_bst(int *array, size_t size)
{
(void)array;
(void)size;
return (NULL);
}
