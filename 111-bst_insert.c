#include "binary_trees.h"

/**
 * bst_insert - a function
 *
 * @tree: binary tree to go through
 * @value: value
 *
 * Return: If tree is NULL, do nothing
 */
bst_t *bst_insert(bst_t **tree, int value)
{
(void)tree;
(void)value;
return (NULL);
}
